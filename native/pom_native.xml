<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>t2linux</groupId>
    <artifactId>iso-native</artifactId>
    <version>0.7</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <main.class>iso.Main</main.class>
        <native.image.name>T2ISO-native</native.image.name>
        <native.maven.plugin.version>0.10.3</native.maven.plugin.version>
        <javafx.version>21.0.6</javafx.version>
        <skip.native.strip>false</skip.native.strip>
    </properties>

    <build>
        <sourceDirectory>${project.basedir}/../src/main/java</sourceDirectory>
        <testSourceDirectory>${project.basedir}/../src/test/java</testSourceDirectory>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${main.class}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.graalvm.buildtools</groupId>
                <artifactId>native-maven-plugin</artifactId>
                <version>${native.maven.plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <imageName>${native.image.name}</imageName>
                    <mainClass>${main.class}</mainClass>
                    <fallback>false</fallback>
                    <verbose>true</verbose>
                    <buildArgs>
                        <buildArg>--no-fallback</buildArg>
                        <buildArg>-Os</buildArg>
                        <buildArg>--enable-url-protocols=http,https</buildArg>
                        <buildArg>--initialize-at-run-time=javafx.application.Platform</buildArg>
                        <buildArg>-H:IncludeLocales=en</buildArg>
                        <!-- old size-heavy option kept for reference: <buildArg>-H:+AddAllCharsets</buildArg> -->
                    </buildArgs>
                </configuration>
                <executions>
                    <execution>
                        <id>build-native</id>
                        <phase>package</phase>
                        <goals>
                            <goal>compile-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>strip-native-artifacts</id>
                        <phase>package</phase>
                        <configuration>
                            <skip>${skip.native.strip}</skip>
                            <target>
                                <exec executable="sh" failonerror="false" resultproperty="native.strip.exit.code">
                                    <arg value="-c"/>
                                    <arg value="if command -v strip >/dev/null 2&gt;&amp;1; then if [ -f '${project.build.directory}/${native.image.name}' ]; then strip --strip-all '${project.build.directory}/${native.image.name}' || true; fi; find '${project.build.directory}' -maxdepth 1 -type f \\( -name '*.so' -o -name '*.dylib' \\) -exec strip --strip-unneeded {} + || true; fi"/>
                                </exec>
                                <echo>strip step exit code: ${native.strip.exit.code}</echo>
                            </target>
                        </configuration>
                        <goals><goal>run</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-base</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <dependency>
            <groupId>org.jfxtras</groupId>
            <artifactId>jmetro</artifactId>
            <version>11.6.16</version>
        </dependency>
        <dependency>
            <groupId>io.github.mkpaz</groupId>
            <artifactId>atlantafx-base</artifactId>
            <version>2.0.1</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
